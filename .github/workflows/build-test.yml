name: Build and test the Docker image

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

env:
  PROJECT_NAME: themeforest-automation-ci

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: docker build . --file Dockerfile --tag ${{ env.PROJECT_NAME }}
      - name: Test
        run: docker run --name ${{ env.PROJECT_NAME }} ${{ env.PROJECT_NAME }} /tmp/tests/run.sh
